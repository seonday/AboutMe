const greet = document.getElementById('greetText');

// darkmode 적용할 elements
const timeElement = document.getElementById('time');
const settingBox = document.getElementById('settingBox');
const footer = document.getElementById('footer');
const whiteBoxSpan = document.querySelectorAll('.textStyle span');
const goalRef = document.getElementsByClassName('goalRef');
